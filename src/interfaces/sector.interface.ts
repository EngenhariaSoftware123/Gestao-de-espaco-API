export interface ISectorCreate{
  name: string
  contact: string 
  acronym: string
  spaceId: number;
  userId: number;
}
export interface ISectorData{
  name: string
  contact: string 
  acronym: string
  spaceId: number;
  email: string;
}