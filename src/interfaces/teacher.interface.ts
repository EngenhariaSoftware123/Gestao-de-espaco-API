export interface ITeacherCreate{
  name: string;
  enrollment: string;
  contact: string;
  userId: number;
  department: string;
  subject: string;
}
export interface ITeacherData{
  name: string;
  enrollment: string;
  contact: string;
  email: string;
  department: string;
  subject: string;
}